trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
# 1️⃣ Installera rätt .NET SDK
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '9.0.x'

# 2️⃣ Installera NuGet
- task: NuGetToolInstaller@1

# 3️⃣ Kontrollera workloads
- script: dotnet workload list
  displayName: 'Lista .NET workloads'

# 4️⃣ Uppdatera workloads (om det behövs)
- script: dotnet workload update
  displayName: 'Uppdatera .NET workloads'

# 5️⃣ Återställ beroenden med `dotnet restore`
- task: DotNetCoreCLI@2
  inputs:
    command: 'restore'
    arguments: '--force'
    projects: '**/*.csproj'

# 6️⃣ Bygg applikationen
- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    arguments: '--configuration Release'
    projects: '**/*.csproj'

# 🚀 7️⃣ Publicera applikationen direkt till en ZIP-fil
- task: DotNetCoreCLI@2
  inputs:
    command: 'publish'
    arguments: '--configuration Release --output $(Build.ArtifactStagingDirectory)/BlackJack'

# 🗜️ 8️⃣ Skapa en ZIP av hela publiceringen
- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(Build.ArtifactStagingDirectory)/BlackJack'
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/BlackJack.zip'

# 📦 9️⃣ Publicera ZIP-filen som en artifact
- task: PublishBuildArtifacts@1
  inputs:
    pathToPublish: '$(Build.ArtifactStagingDirectory)/BlackJack.zip'
    artifactName: 'drop'
    publishLocation: 'Container'

# 🚀 🔟 Deploya ZIP-filen direkt till Azure Web App
- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'webApp'
    appType: 'webApp'
    WebAppName: 'BlackJack'
    package: '$(Build.ArtifactStagingDirectory)/BlackJack.zip'
